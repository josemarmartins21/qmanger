<div class="p-5 bg-[var(--fundo-card)] dark:bg-[var(--dark-fundo-card)] shadow-[var(--card-sombras)] dark:shadow-[var(--dark-card-sombras)] rounded-3xl flex justify-between">
    <p class="sm:font-semibold">{{ $setting }}</p>

    <div>
        {{ $change }}
    </div>
</div>